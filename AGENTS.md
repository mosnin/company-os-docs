# Company OS documentation

The public documentation must track the canonical mosnin/companyos contract and
mosnin/company-os-web implementation. Kernel protocol changes must update the
Private kernels page, manifest schema and repository guide together. Never copy
private kernel contents or credentials here. Do not imply registry publication,
live deployment or remote filesystem verification from code or client receipts.
Run npm run check before release. Preserve docs source as durable repository data.
